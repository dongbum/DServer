language: cpp

dist: xenial

compiler:
- gcc

os:
- linux

before_install:
# - sudo apt-cache search mysql
- sudo apt-get install libjemalloc-dev libjemalloc1 libjemalloc1-dbg libjsoncpp-dev libjson0 libjson0-dev libboost-dev libmysqlcppconn-dev libmysql++-dev


script:
- g++ src/game_server/main.cpp -std=c++11 -o DServer
- ./DServer