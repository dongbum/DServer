language: cpp

dist: trusty

compiler:
- gcc

os:
- linux

before_install:
- sudo apt-cache search mysql
- sudo apt-get install libjemalloc-dev libjsoncpp-dev libjson0 libjson0-dev libboost-dev libmysqlcppconn-dev libmysqlcppconn7 libmysql++-dev libmysql++3


script:
- g++ src/game_server/main.cpp -std=c++11 -o DServer
- ./DServer